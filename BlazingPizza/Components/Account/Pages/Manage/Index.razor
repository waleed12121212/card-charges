@page "/Account/Manage"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/Account/Manage/Profile", true);
    }
} 